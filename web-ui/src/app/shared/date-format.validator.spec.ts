import { DateFormatValidator } from './date-format.validator';

describe('DateFormatValidator', () => {
  it('should create an instance', () => {
    expect(new DateFormatValidator()).toBeTruthy();
  });
});

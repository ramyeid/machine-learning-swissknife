import { ValidationMessageGenerator } from './validation-message-generator';

describe('ValidationMessageGenerator', () => {
  it('should create an instance', () => {
    expect(new ValidationMessageGenerator()).toBeTruthy();
  });
});
